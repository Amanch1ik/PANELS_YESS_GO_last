# Example environment file for Partner Panel (copy to partner-panel/.env before building)

# API base used by the built frontend (use '/api' when serving behind a proxy)
VITE_API_BASE=/api

# Production API full URL (optional — prefer proxying /api in production)
# VITE_API_BASE_PROD=https://api.yessgo.org/api/v1

# App base path (if serving from a subpath)
VITE_APP_BASE=/

# Development mode flag (true to enable dev-only behaviors)
VITE_DEV_MODE=false

# Optional monitoring / analytics
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0


