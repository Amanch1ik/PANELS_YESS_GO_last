# 🔧 Production .env template for server deployment
# Copy this file to admin-panel/.env and partner-panel/.env on your server
#
# Production environment variables for YESS!GO panels
# Used when running in production (build time). The frontend will use VITE_API_BASE
#
# API Configuration - Direct API calls for production
VITE_API_BASE=https://api.yessgo.org/api/v1

# If you host the API under a custom domain or a different path, change accordingly:
# VITE_API_BASE=https://admin.yessgo.org/api/v1
# VITE_API_BASE=http://localhost:8000/api/v1
# VITE_API_BASE=https://your-custom-api-domain.com/api/v1

# Notes:
# - For development we use the Vite proxy (/api -> https://api.yessgo.org/api/v1)
#   so you typically don't need to set VITE_API_BASE locally.
# - On the production server, create admin-panel/.env and partner-panel/.env
#   with this value before building the app so static bundles reference the correct API.

# Optional: Analytics & Monitoring
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# VITE_SENTRY_DSN=https://xxx@sentry.io/xxx
