# 🔧 Исправление ошибки сборки на сервере

## 🚨 Проблема
```
✘ [ERROR] Cannot start service: Host version "0.21.5" does not match binary version "0.27.2"
```

## 📋 Решение

### Шаг 1: Подключитесь к серверу
```bash
ssh yesgoadm@srv
cd /var/www/PANELS_YESS_GO_last/admin-panel
```

### Шаг 2: Выполните команды исправления
```bash
# Удалите старые зависимости
rm -rf node_modules package-lock.json

# Очистите кэш npm
npm cache clean --force

# Переустановите зависимости
npm install

# Проверьте версии
npm list esbuild vite

# Попробуйте сборку
npm run build
```

### Шаг 3: Альтернативное решение (если не помогло)
```bash
# Принудительная установка конкретной версии esbuild
npm install esbuild@0.21.5 --save-dev --force

# Или очистка и полная переустановка
rm -rf node_modules package-lock.json
npm cache clean --force
npm install --force
npm run build
```

## 🔍 Диагностика

### Проверьте версии на сервере:
```bash
node --version
npm --version
npm list esbuild
npm list vite
```

### Ожидаемые версии:
- Node.js: 18+ или 20+
- npm: 8+
- esbuild: 0.21.5 (совпадает с сервером)
- vite: 5.2.0

## ✅ Проверка успеха

После исправления сборка должна пройти без ошибок:
```bash
npm run build
# Expected output: ✓ built in X.XXs
```

## 📞 Если проблема persists

1. Убедитесь, что на сервере та же версия Node.js
2. Проверьте права доступа к папкам
3. Попробуйте `npm install --unsafe-perm=true` если проблемы с правами
