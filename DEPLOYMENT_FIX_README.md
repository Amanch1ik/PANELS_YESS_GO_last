# 🔧 Исправление ошибок сборки на сервере

## Проблема
При сборке возникает ошибка:
```
EACCES, Permission denied: /var/www/PANELS_YESS_GO_last/admin-panel/dist/assets
```

## Причина
Недостаточно прав доступа для пользователя `yesgoadm` к папке `dist/`.

## Решение 1: Быстрое исправление (рекомендуется)

```bash
cd /var/www/PANELS_YESS_GO_last/admin-panel

# Очистить старую папку dist
rm -rf dist

# Создать новую с правильными правами
mkdir -p dist
chmod 755 dist

# Запустить сборку
npm run build
```

## Решение 2: Полное исправление прав (если Решение 1 не помогло)

```bash
# Для администратора сервера:
sudo chown -R yesgoadm:yesgoadm /var/www/PANELS_YESS_GO_last
sudo find /var/www/PANELS_YESS_GO_last -type d -exec chmod 755 {} \;
sudo find /var/www/PANELS_YESS_GO_last -type f -exec chmod 644 {} \;

# Или запустить скрипт:
bash fix-server-permissions.sh
```

## Решение 3: Использовать sudo для сборки

```bash
cd /var/www/PANELS_YESS_GO_last/admin-panel
sudo npm run build
```

## Проверка после исправления

После успешной сборки должны появиться файлы:
- `dist/index.html`
- `dist/assets/` (папка с JS/CSS файлами)
- `dist/manifest.json`
- `dist/favicon.svg`

## Профилактика

Чтобы избежать подобных проблем в будущем:
1. Всегда используйте одного пользователя для deployment
2. Проверяйте права после копирования файлов
3. Добавьте эти команды в CI/CD pipeline

## Контакт

Если проблема не решается, сообщите логи команды:
```bash
ls -la /var/www/PANELS_YESS_GO_last/admin-panel/dist/
whoami
groups
```
